---
import { Image } from 'astro:assets';
import Badge from "@/components/Badge.astro";
import FlipCard from "@/components/FlipCard.astro";
import bgImg from "@/assets/Vision/Collages USM 2.png";
import imgProfesorado from "@/assets/Pilares/1.png";
import imgFormacion from "@/assets/Pilares/2.png";
import imgComunidad from "@/assets/Pilares/3.png";

const pilares = [
  {
    title: "Liderazgo del Profesorado",
    body: "El liderazgo académico debe orientar el futuro de la Universidad. Fortaleceremos el desarrollo del profesorado, la investigación y una gestión institucional que facilite su labor.",
    image: imgProfesorado,
    color: "#f7ae00",
  },
  {
    title: "Formación para Transformar",
    body: "Formaremos profesionales íntegros, preparados para los desafíos del siglo XXI, mediante una experiencia universitaria de excelencia y aprendizaje permanente.",
    image: imgFormacion,
    color: "#d60019",
  },
  {
    title: "Una Comunidad para el Desarrollo de Chile",
    body: "Integraremos a exalumnos, industria y comunidad para fortalecer el impacto de la UTFSM, potenciar la colaboración y proyectar a la Universidad hacia Chile y el mundo.",
    image: imgComunidad,
    color: "#004b85",
  },
];
---

<section class="relative w-full flex items-center overflow-hidden" id="pilares">
  <!-- Fondo -->
  <Image
    src={bgImg}
    alt="Campus UTFSM"
    class="absolute inset-0 w-full h-full object-cover"
  />
  <!-- Overlay -->
  <div class="absolute inset-0 bg-black/40"></div>

  <div class="relative z-10 w-full max-w-7xl mx-auto px-6 sm:px-8 lg:px-12 py-12 md:py-16">
    <!-- Título -->
    <div class="flex flex-col gap-2 mb-8 lg:mb-10">
      <Badge text="Pilares" variant="blanca" />
      <h2 class="text-4xl sm:text-5xl md:text-6xl leading-[1.05] tracking-tight text-white">
        <span class="font-bold">Tres pilares,</span><br />
        <span class="font-normal text-brand-amarillo">una visión</span>
      </h2>
    </div>

    <!-- Tarjetas -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-5 lg:gap-6">
      {pilares.map((pilar) => (
        <FlipCard
          class="w-full h-[300px] md:h-[380px] rounded-2xl shadow-xl text-left"
          aria-label={`Ver más sobre ${pilar.title}`}
        >
          <!-- Fondo de tarjeta -->
          <div slot="background" class="relative w-full h-full">
            <Image
              src={pilar.image}
              alt={pilar.title}
              class="absolute inset-0 w-full h-full object-cover"
            />
            <div
              class="absolute inset-0 pointer-events-none"
              style={`background: linear-gradient(to top, ${pilar.color} 0%, ${pilar.color}dd 45%, ${pilar.color}80 70%, transparent 100%);`}
            ></div>
          </div>

          <!-- Frente -->
          <div slot="front" class="absolute inset-0 flex items-end p-5 md:p-6">
            <div class="flex items-start gap-3">
              <span class="w-1.5 h-16 bg-white/80 rounded-full shrink-0"></span>
              <h3 class="text-2xl md:text-3xl font-bold leading-tight text-white">
                {pilar.title}
              </h3>
            </div>
          </div>

          <!-- Reverso -->
          <div
            slot="back"
            class="absolute inset-0 flex flex-col justify-center p-5 md:p-6"
            style={`background: linear-gradient(to top, ${pilar.color} 0%, ${pilar.color}ee 60%, ${pilar.color}cc 100%);`}
          >
            <h3 class="text-xl md:text-2xl font-bold leading-tight text-white mb-3">
              {pilar.title}
            </h3>
            <p class="text-sm md:text-base leading-relaxed text-white/95">
              {pilar.body}
            </p>
          </div>
        </FlipCard>
      ))}
    </div>
  </div>
</section>
